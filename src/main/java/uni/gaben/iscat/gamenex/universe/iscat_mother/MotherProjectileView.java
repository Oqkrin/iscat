package uni.gaben.iscat.gamenex.universe.iscat_mother;

import javafx.scene.canvas.GraphicsContext;
import uni.gaben.iscat.gamenex.lib.abstracts.AbstractEntityView;
import uni.gaben.iscat.gamenex.lib.interfaces.view.Drawable;
import uni.gaben.iscat.utils.ThemeColors;
/*
public class MotherProjectileView extends AbstractEntityView<MotherProjectile> implements Drawable<MotherProjectile> {
    @Override
    protected void drawContent(MotherProjectile entity, GraphicsContext gc, double x, double y, double width, double height) {
        gc.save();
        gc.setFill(ThemeColors.getColorError());
        drawDebugCollision(entity, gc);
        gc.restore();
    }

    @Override
    public void draw(MotherProjectile entity, GraphicsContext gc) {
        setupGraphicsContextAndDrawContent(entity, gc, 0.0);
    }
}
*/