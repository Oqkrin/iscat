package uni.gaben.iscat;

/**
 * Costanti globali dell'applicazione (font, configurazioni della finestra, layout, ecc.).
 */
public final class IscatSettings {

    /** Costruttore privato per impedire l'istanziazione di una classe di sole costanti. */
    private IscatSettings() {}

    /* Layout */
    /** Unità di misura standard utilizzata come base per il calcolo proporzionale di spaziature, padding e margini nella UI. */
    public static final double STANDARD_UNIT = 16.0;

}